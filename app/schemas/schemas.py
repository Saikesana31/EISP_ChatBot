"""
app/schemas/schemas.py — Pydantic models for request/response validation and documentation.

* All pydantic models : LoginRequest, TokenResponse, UserProfile, TokenPayload, 
                        EmployeeListResponse,statusupdateRequest, ChaMessageRequest,
                        Alertlistresponse  
    
* FastAPI uses these models  to validate incoming request bodies and to generate the swagger /docs.    

"""
    